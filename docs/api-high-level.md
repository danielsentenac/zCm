\page api_high_level High-Level API

Process-oriented helpers that expose config-driven runtime behavior.

This module is intended for application code that wants to launch and manage
`zcm_proc` style services with minimal boilerplate.

API reference:
- \ref zcm_high_level
